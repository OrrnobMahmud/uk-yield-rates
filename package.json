{
  "name": "uk-yield-rates",
  "version": "1.3.1",
  "description": "WordPress plugin for displaying UK gilt yield rates",
  "scripts": {
    "build": "wp-scripts build blocks/yield-rates/index.js --output-path=blocks/yield-rates/dist",
    "start": "wp-scripts start blocks/yield-rates/index.js --output-path=blocks/yield-rates/dist",
    "lint": "wp-scripts lint-js blocks/yield-rates/",
    "lint:fix": "wp-scripts lint-js blocks/yield-rates/ --fix"
  },
  "devDependencies": {
    "@wordpress/scripts": "^28.0.0"
  },
  "author": "Orrnob Mahmud Local SEO Strategist",
  "license": "GPL-2.0-or-later",
  "keywords": [
    "wordpress",
    "plugin",
    "yield-rates",
    "gilts",
    "bonds",
    "finance"
  ],
  "dependencies": {
    "@babel/runtime": "^8.0.0"
  }
}
